// Ejemplo básico con array
let lenguajes = ['Javascript', 'Python', 'Typescript'];

for ( let lenguaje of lenguajes ) {
    console.log(`Ejeemplo de for...of con array: ${lenguaje}`);
}

console.log(`\n------------------------------\n`);

// Ejemplo con cadena de texto
let saludo = 'Hola';

for ( let letra of saludo) {
    console.log(`Ejemplo de for... of con string: ${letra}`);
}